const Footer = () => {
  return (
    <footer className="bg-blue-800 text-white text-center py-4 mt-6">
      &copy; {new Date().getFullYear()} Todo App | Built with React & Tailwind CSS
    </footer>
  );
};

export default Footer;
